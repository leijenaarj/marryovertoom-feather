---
import BaseHead from '../components/BaseHead.astro';
import OpenGraphImage from '../components/OpenGraphImage.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title={`OpenGraph Image Generator | ${SITE_TITLE}`}
      description="Generate OpenGraph images for your content"
    />
    <style>
      .checkerboard {
        background-image: linear-gradient(45deg, #e5e5e5 25%, transparent 25%),
          linear-gradient(-45deg, #e5e5e5 25%, transparent 25%),
          linear-gradient(45deg, transparent 75%, #e5e5e5 75%),
          linear-gradient(-45deg, transparent 75%, #e5e5e5 75%);
        background-size: 20px 20px;
        background-position:
          0 0,
          0 10px,
          10px -10px,
          -10px 0px;
        background-color: #f5f5f5;
      }
    </style>
  </head>
  <body class="bg-stone-100 min-h-screen">
    <!-- Header -->
    <div class="max-w-[1400px] mx-auto px-4 py-8">
      <div class="mb-8">
        <h1 class="font-primary text-3xl text-stone-900 mb-4">OpenGraph Image Preview</h1>
        <p class="text-stone-600">
          This preview is shown at the actual size (1200×630 pixels) for accurate screenshots.
        </p>
      </div>
    </div>

    <!-- Preview Container -->
    <div class="max-w-[1400px] mx-auto">
      <div class="checkerboard p-24">
        <div class="mx-auto w-fit bg-transparent">
          <OpenGraphImage
            title="Minimal Blog Theme for Astro"
            description="Clean typography, subtle design elements, and a focus on readability. Built with Astro v5.0 & Tailwind CSS v4.0."
            label="astrothem.es"
          />
        </div>
      </div>
    </div>

    <!-- Instructions -->
    <div class="max-w-[1400px] mx-auto px-4 py-8">
      <div class="p-6 bg-white rounded-xl">
        <h2 class="font-primary text-2xl text-stone-900 mb-4">How to Use</h2>
        <ol class="space-y-4 text-stone-600">
          <li>
            1. Take a screenshot of the preview above (it's already at the correct size: 1200×630
            pixels)
          </li>
          <li>2. Use the image in your meta tags:</li>
          <pre
            class="p-4 bg-stone-50 rounded-lg text-sm font-mono text-stone-700 mt-2">
&lt;meta property="og:image" content="/path/to/your/image.png" /&gt;
&lt;meta name="twitter:image" content="/path/to/your/image.png" /&gt;</pre>
        </ol>
      </div>
    </div>
  </body>
</html>
